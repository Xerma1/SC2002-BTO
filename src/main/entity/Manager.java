package main.entity;

public class Manager extends User {

    //constructor
    public Manager(String name, String userID, int age, boolean married, String password, String accessLevel) {
        super(name, userID, age, married, password, accessLevel);
    }
}
